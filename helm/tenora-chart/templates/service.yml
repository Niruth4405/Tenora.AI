apiVersion: v1
kind: Service
metadata:
  name: tenora-app
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: tenora-app     
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.targetPort }}
      targetPort: {{ .Values.service.targetPort }}